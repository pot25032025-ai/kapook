# วิธีเปิดบอร์ดสาธารณะ (ทุกคนเห็นกระทู้ร่วมกัน)

เว็บบอร์ดต้องมี "ฐานข้อมูลกลาง" ถึงจะให้ทุกคนเห็นโพสต์เดียวกันได้
เราจะใช้ **Firebase Firestore** ของ Google ซึ่งฟรีสำหรับเว็บขนาดเล็ก ใช้เวลาตั้งค่าราว 10 นาที

## ขั้นตอน

### 1. สร้างโปรเจกต์ Firebase
1. เข้า https://console.firebase.google.com แล้วล็อกอินด้วยบัญชี Google
2. กด **Create a project** ตั้งชื่อ เช่น `lanpattaya`
3. ปิด Google Analytics ก็ได้ (ไม่จำเป็น) แล้วกดสร้างจนเสร็จ

### 2. เปิดใช้ Firestore
1. เมนูซ้าย → **Build → Firestore Database** → กด **Create database**
2. เลือกโหมด **Start in production mode** และเลือก location ใกล้ไทย เช่น `asia-southeast1` (สิงคโปร์)

### 3. ตั้งกติกาความปลอดภัย (สำคัญ)
ไปที่แท็บ **Rules** ของ Firestore แล้ววางแทนทั้งหมดด้วย:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /threads/{id} {
      // ใครก็อ่านได้
      allow read: if true;
      // ตั้งกระทู้ได้ ถ้าข้อมูลครบและไม่ยาวเกินกำหนด
      allow create: if request.resource.data.title is string
        && request.resource.data.title.size() > 0
        && request.resource.data.title.size() <= 120
        && request.resource.data.body is string
        && request.resource.data.body.size() <= 4000
        && request.resource.data.author is string
        && request.resource.data.author.size() <= 40
        && request.resource.data.replies is list
        && request.resource.data.replies.size() == 0;
      // ตอบกระทู้ = เพิ่มคำตอบได้ทีละ 1 เท่านั้น (แก้/ลบของเดิมไม่ได้)
      allow update: if request.resource.data.replies.size() == resource.data.replies.size() + 1
        && request.resource.data.title == resource.data.title
        && request.resource.data.body == resource.data.body
        && request.resource.data.author == resource.data.author;
      // ห้ามลบ
      allow delete: if false;
    }
  }
}
```

กด **Publish**

### 4. เอาค่า config มาใส่ในเว็บ
1. กดไอคอนฟันเฟือง → **Project settings** → เลื่อนลงหา **Your apps**
2. กดไอคอน **</>** (Web) ตั้งชื่อแอปอะไรก็ได้ → กด Register
3. จะได้ก้อนโค้ด `const firebaseConfig = { apiKey: "...", ... }`
4. เปิดไฟล์ `index.html` หาบรรทัด `window.FIREBASE_CONFIG = null;`
   แล้วเปลี่ยนเป็น:

```js
window.FIREBASE_CONFIG = {
  apiKey: "ค่าจริงของคุณ",
  authDomain: "ค่าจริงของคุณ",
  projectId: "ค่าจริงของคุณ",
  storageBucket: "ค่าจริงของคุณ",
  messagingSenderId: "ค่าจริงของคุณ",
  appId: "ค่าจริงของคุณ"
};
```

(หรือส่งก้อน config มาให้ผู้ช่วยใส่ให้ก็ได้ — ค่าเหล่านี้เปิดเผยได้ ไม่ใช่ความลับ
ความปลอดภัยจริงอยู่ที่ Rules ในข้อ 3)

### 5. อัปโหลด index.html ทับใน GitHub
รอ 1-2 นาที เปิดเว็บ → แถบเตือนสีเหลือง "โหมดทดลอง" จะหายไป
และข้อความท้ายเว็บจะเปลี่ยนเป็น "กระทู้เป็นสาธารณะ" — จากนั้นทุกคนที่เข้าเว็บ
จะเห็นกระทู้เดียวกันทั้งหมด

## ข้อควรรู้

- **กระทู้เดิมใน localStorage จะไม่ย้ายไปเอง** เพราะเป็นข้อมูลคนละที่กัน ถือว่าเริ่มบอร์ดใหม่
- **บอร์ดเปิดให้โพสต์โดยไม่ต้องล็อกอิน** สะดวกแต่ก็เสี่ยงสแปม ถ้าเว็บโตขึ้น
  ควรเพิ่มระบบล็อกอิน (Firebase Authentication) และปุ่มรายงาน/ลบสำหรับแอดมิน
- **โควตาฟรีของ Firestore**: อ่าน 50,000 ครั้ง/วัน เขียน 20,000 ครั้ง/วัน
  เหลือเฟือสำหรับเว็บเริ่มต้น
